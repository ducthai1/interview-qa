import type { Question } from '../types'

export const systemDesignFeQuestions: Question[] = [
  {
    id: 'sd-001',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'mcq',
    question: 'When designing a component library, what are the key principles that distinguish a well-architected design system from a collection of components?',
    options: [
      'A design system just has more components than a component library',
      'A design system includes: design tokens (color, spacing, typography as single source of truth), component variants with consistent API patterns, accessibility built-in, documentation with usage guidelines, versioning strategy, and cross-team governance — not just UI components',
      'A design system must be built with Storybook and published to npm',
      'Design systems are only needed for companies with more than 10 frontend developers',
    ],
    answer: 1,
    explanation: 'Design systems are multi-layered: (1) Foundation: design tokens (CSS variables or JS objects), (2) Components: accessible, composable UI, (3) Patterns: composition guidelines, (4) Documentation: Storybook or similar, (5) Governance: contribution process, breaking change policy. Without tokens, you have components but not a system — the "system" is the shared vocabulary enforcing consistency.',
    tags: ['design-system', 'architecture', 'tokens'],
    year: 2025,
  },
  {
    id: 'sd-002',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'system-design',
    question: 'Design the architecture for a micro-frontend application where 3 teams independently own different sections of a dashboard. How would you handle routing, shared state, and style isolation?',
    options: [
      'Use iframes for full isolation between team sections',
      'Use Module Federation: each team deploys independently as a remote. Shell app orchestrates routing and provides shared layout. Shared state via custom events or a shared store loaded via singleton. CSS isolation via Shadow DOM or strict BEM naming conventions with team prefixes.',
      'Build a monolith and divide it by React context providers',
      'Each team deploys a separate Next.js app with its own domain',
    ],
    answer: 1,
    explanation: 'Micro-frontend architecture decisions: (1) Integration: Module Federation (runtime), npm packages (compile-time), iframes (full isolation). (2) Routing: shell router, each MFE handles sub-routes. (3) Shared state: avoid shared mutable state across MFEs — prefer custom events or URL state; for unavoidable sharing, use a singleton mediator. (4) Styles: Shadow DOM for full isolation, or CSS Modules/BEM with team namespace prefix. (5) Communication: custom events over direct imports.',
    tags: ['micro-frontends', 'module-federation', 'architecture'],
    year: 2025,
  },
  {
    id: 'sd-003',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'system-design',
    question: 'Design an API layer for a React application that handles authentication, request caching, error handling, and request cancellation. What abstractions would you create?',
    options: [
      'Just use fetch() directly everywhere in components',
      'Create: (1) An HTTP client layer (axios instance with interceptors for auth headers, error handling, token refresh), (2) TanStack Query for data fetching/caching with query key factories, (3) MSW handlers for testing, (4) TypeScript-typed API functions, (5) AbortController integration for cancellation.',
      'Use React Context to store a global fetch function',
      'Create a single monolithic api.ts file with all endpoints as functions',
    ],
    answer: 1,
    explanation: 'Layered API architecture: HTTP Client (axios instance) → interceptors add auth headers, handle 401 → refresh token → retry. API service functions (typed): getUserById(id: string): Promise<User>. TanStack Query: query key factory (userKeys.detail(id)) for consistent cache invalidation. AbortController passed to requests, connected to TanStack Query\'s enabled+queryKey changes. Type-safe error handling with discriminated unions.',
    tags: ['api-layer', 'architecture', 'axios', 'tanstack-query'],
    year: 2025,
  },
  {
    id: 'sd-004',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'mcq',
    question: 'What is a monorepo architecture and what are the main trade-offs compared to polyrepo?',
    options: [
      'Monorepo = one repo with one app; polyrepo = multiple repos each with one app',
      'Monorepo: all projects in one repository — enables atomic cross-project changes, shared tooling/lint config, easier refactoring, and local package linking. Trade-offs: slower CI without task orchestration (Turborepo/Nx), complex git history, requires tooling investment. Polyrepo: full team autonomy, simpler per-repo setup, but cross-repo changes require multiple PRs and versioned releases.',
      'Monorepo requires all teams to use the same technology stack',
      'Monorepos are only suitable for single-framework applications',
    ],
    answer: 1,
    explanation: 'Monorepo wins: atomic commits across shared libraries and apps, one lint/TS config to maintain, easy local package linking (workspace:*), no publish step during development, cross-team visibility. Polyrepo wins: independent deployment cadences, smaller repos for IDE performance, true team autonomy. The trend in 2024-2025 is toward monorepos with Turborepo/Nx for most teams building multiple related products.',
    tags: ['monorepo', 'polyrepo', 'architecture'],
    year: 2025,
  },
  {
    id: 'sd-005',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'system-design',
    question: 'Design the state architecture for a complex multi-step form (checkout flow) with 5 steps, cross-step validation, and the ability to go back and edit previous steps.',
    options: [
      'Use a single useState object with all form fields',
      'Use a state machine (XState) or stepwise reducer: define states (billing → shipping → payment → review → confirmation), transitions between steps, validation guards preventing forward progression, and persisted context. Alternatively: React Hook Form with Zod validation per step, combined with URL params for step tracking.',
      'Store each step in separate React Context providers',
      'Use localStorage to save form state between steps',
    ],
    answer: 1,
    explanation: 'Complex multi-step form architecture: (1) State machine ideal for explicit valid transitions; XState models: idle → filling → validating → confirmed, with back transitions. (2) React Hook Form: useForm with a shared schema, each step registers fields, trigger("stepFields") validates current step before advancing. (3) URL params for current step (shareable, back-button support). (4) Session storage for persistence across refreshes without premature submission.',
    tags: ['state-machines', 'forms', 'xstate', 'architecture'],
    year: 2025,
  },
  {
    id: 'sd-006',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'system-design',
    question: 'How would you design the real-time features for a collaborative document editor (like Notion) from a frontend architecture perspective?',
    options: [
      'Poll the server every 100ms for changes',
      'Use WebSocket (or SSE for read-only) with a CRDT or OT algorithm for conflict resolution. Frontend: operational transforms applied optimistically, sync with server via WebSocket, Yjs for CRDT implementation. Presence (who is viewing/editing) via separate presence channel. Offline support via IndexedDB + sync on reconnect.',
      'Use Server-Sent Events for all bidirectional communication',
      'Store changes in React state and batch-send to server every 5 seconds',
    ],
    answer: 1,
    explanation: 'Collaborative editor stack: WebSocket (Socket.io or native) for bidirectional sync. CRDT (Conflict-free Replicated Data Type) via Yjs handles concurrent edits without server-side merge — crucial for offline support. Awareness API for cursor/presence. Persistence: IndexedDB for offline, sync delta on reconnect. React integration: use useY() hooks from y-react. Alternative: operational transforms (OT) with server authority, harder to implement.',
    tags: ['websocket', 'real-time', 'crdt', 'collaboration'],
    year: 2025,
  },
  {
    id: 'sd-007',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'system-design',
    question: 'Design the internationalization (i18n) architecture for a Next.js application supporting 20 languages with dynamic content and plural rules.',
    options: [
      'Use conditional rendering with if/else for each language',
      'Use next-intl or react-i18next with: locale routing (/en/, /fr/), namespace-based translation files loaded per-route, ICU message format for plurals/gender/interpolation, server-side translation in RSC, locale detection from Accept-Language header, RTL support with CSS logical properties.',
      'Store translations in a database and fetch on every render',
      'Translate strings with a third-party API at render time',
    ],
    answer: 1,
    explanation: 'Production i18n: (1) Routing: Next.js locale routing with middleware for detection. (2) Translation files: per-namespace JSON (common, checkout, profile) for code-splitting. (3) Format: ICU message format handles plurals ({count, plural, one {# item} other {# items}}), dates, currencies via Intl API. (4) RSC: use next-intl getTranslations() in server components. (5) Ci: automated translation file validation, pseudo-localization for layout testing.',
    tags: ['i18n', 'nextjs', 'architecture', 'localization'],
    year: 2025,
  },
  {
    id: 'sd-008',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'mcq',
    question: 'What is the "Compound Component" pattern and when should you use it for component library design?',
    options: [
      'Compound components are components that use multiple state management libraries',
      'A pattern where a parent component manages shared state/context and child sub-components access it implicitly. Example: <Select><Select.Option><Select.Trigger> — consumers control composition without prop drilling. Ideal for: menus, accordions, tabs, modals with multiple coordinated parts.',
      'Compound components are components that extend multiple base classes',
      'A pattern for creating responsive components that work at multiple breakpoints',
    ],
    answer: 1,
    explanation: 'Compound components: parent uses Context to share state; sub-components (static properties or named exports) read the context. Example: <Tabs> provides selectedTab context; <Tabs.List>, <Tabs.Tab>, <Tabs.Panel> consume it. Benefits: flexible composition without prop drilling, explicit API surface, consumers control rendering order. Used by Radix UI, Headless UI, React Aria. Prefer Radix for complex cases over building from scratch.',
    tags: ['compound-components', 'component-patterns', 'design-system'],
    year: 2025,
  },
  {
    id: 'sd-009',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'mcq',
    question: 'What is offline-first architecture and what technologies enable it on the frontend?',
    options: [
      'Offline-first means the app works without a backend server',
      'Offline-first means the app is designed to work with cached data by default, syncing to the server when connectivity is available. Technologies: Service Workers (network interception/caching), IndexedDB (structured offline storage), Background Sync API, Cache API, TanStack Query with persistence adapter.',
      'Offline-first is only possible with native mobile apps',
      'Offline-first requires a local server running on the user\'s machine',
    ],
    answer: 1,
    explanation: 'Offline-first stack: (1) Service Worker: intercepts fetch requests, serves from cache when offline (Cache API), uses strategies like stale-while-revalidate. (2) IndexedDB: stores structured data (user data, draft forms, cached API responses). (3) Background Sync: queues actions (form submissions) while offline, retries when connected. (4) Workbox: higher-level SW library. React Query + idb-keyval: persists server state cache to IndexedDB.',
    tags: ['offline-first', 'service-worker', 'indexeddb', 'pwa'],
    year: 2025,
  },
  {
    id: 'sd-010',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'system-design',
    question: 'How would you architect a frontend component library that needs to support both React and Vue consumers from the same codebase?',
    options: [
      'Build separate libraries and manually keep them in sync',
      'Use Web Components (Custom Elements) as the framework-agnostic layer: build components as custom elements using Lit or native APIs. Framework-specific wrapper packages (react-my-lib, vue-my-lib) add reactive bindings and TypeScript types. This enables one canonical component implementation used by all frameworks.',
      'Write the entire library twice in both React and Vue',
      'Use a shared CSS library and have each framework team add their own JS logic',
    ],
    answer: 1,
    explanation: 'Cross-framework component strategy: (1) Web Components (Lit): write once, wrap for each framework. React wrappers handle event naming (camelCase ↔ kebab-case). (2) Alternative: headless logic in vanilla JS/TS (like Floating UI, TanStack Table), framework-specific packages add rendering. (3) Figma tokens → Style Dictionary → CSS variables as the design layer that works everywhere. Trade-off: Web Components have SSR limitations; headless approach is more flexible.',
    tags: ['web-components', 'cross-framework', 'component-library', 'architecture'],
    year: 2025,
  },
  {
    id: 'sd-011',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'mcq',
    question: 'What is the "Render Props" pattern vs "Component Composition" and when should you prefer one over the other?',
    options: [
      'They are identical patterns with different names',
      'Render Props: parent component calls a function prop to render children, enabling logic sharing across different UI structures. Component Composition: pass components as children/props for slot-based flexibility. Prefer composition for layout/structure; prefer render props (or hooks) when sharing complex logic with flexible rendering.',
      'Render Props are deprecated in favor of hooks',
      'Component Composition only works with Class Components',
    ],
    answer: 1,
    explanation: 'Render Props (<DataFetcher render={({ data }) => <UI data={data} />}>) and custom hooks both share stateful logic. Hooks replaced most Render Props use cases for logic sharing. When to still use render props: when the shared logic needs to render dynamic arbitrary children (Virtualized list row renderer, form field wrapper that renders validation state around any input). Composition (children prop) is for layout/slot patterns.',
    tags: ['render-props', 'composition', 'component-patterns'],
    year: 2025,
  },
  {
    id: 'sd-012',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'mcq',
    question: 'What are design tokens and how should they be structured in a large design system?',
    options: [
      'Design tokens are authentication tokens for design tools like Figma',
      'Design tokens are platform-agnostic named design values (colors, spacing, typography) that represent the single source of truth. Structure: Global tokens (raw values: color-blue-500: #3b82f6) → Semantic tokens (role-based: color-brand-primary: {color.blue.500}) → Component tokens (component-specific: button-bg-primary: {color.brand.primary}).',
      'Design tokens are CSS custom properties only',
      'Tokens are only used for color; spacing and typography are handled separately',
    ],
    answer: 1,
    explanation: 'Three-tier token architecture (W3C Design Token Community Group standard): (1) Primitive/Global: raw scale values — spacing-4: 16px, color-blue-500: #3b82f6. (2) Semantic/Alias: meaning-driven — color-background-primary: {color.neutral.0}, color-interactive-default: {color.blue.500}. (3) Component: component-specific — button-background: {color.interactive.default}. Tools: Style Dictionary transforms tokens to CSS vars, JS constants, iOS/Android formats.',
    tags: ['design-tokens', 'design-system', 'style-dictionary'],
    year: 2025,
  },
  {
    id: 'sd-013',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'mcq',
    question: 'How would you approach transitioning a large monolith frontend to a micro-frontend architecture without a big-bang rewrite?',
    options: [
      'Rewrite everything in 6 months and switch over all at once',
      'Strangler Fig pattern: identify vertical slices (by route/feature), build new MFEs alongside the monolith, route specific paths to new MFEs via nginx or the shell router, gradually strangle the monolith. Feature flags control cut-over. Run both in parallel until full migration.',
      'Extract shared components first, then split the rest later',
      'Freeze feature development for 1 year during migration',
    ],
    answer: 1,
    explanation: 'Strangler Fig for MFE migration: (1) Identify seams: natural feature/route boundaries. (2) Create shell application that routes between monolith and new MFEs. (3) Build one MFE slice with new tech (React 18, TypeScript, TanStack Query). (4) Route /new-feature to the MFE, /legacy to the monolith. (5) Extract one feature at a time — teams can ship new features in MFEs while monolith is maintained. Gradually "strangle" the old system.',
    tags: ['micro-frontends', 'migration', 'strangler-fig', 'architecture'],
    year: 2025,
  },
  {
    id: 'sd-014',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'mcq',
    question: 'What patterns ensure CSS isolation in a micro-frontend architecture?',
    options: [
      'Use a single global stylesheet shared by all MFEs',
      'Options by isolation level: (1) Shadow DOM: true CSS encapsulation, prevents global styles from leaking in/out — but limits style inheritance; (2) CSS Modules: scoped class names at build time; (3) Strict BEM with team prefix (team-a__component); (4) CSS-in-JS: runtime scope; (5) Style reset per MFE to prevent inheritance.',
      'CSS isolation is impossible in micro-frontend architectures',
      'Only use inline styles to prevent any CSS conflicts',
    ],
    answer: 1,
    explanation: 'CSS isolation tradeoffs: Shadow DOM = strongest isolation but loses theme inheritance and complicates global font/color tokens. CSS Modules = good for compile-time separation. BEM prefix = simple convention, works everywhere but relies on team discipline. CSS-in-JS = runtime isolation with dynamic theming. Real projects often combine: CSS Modules for component styles + CSS variables for shared design tokens that penetrate shadow DOM boundaries.',
    tags: ['css-isolation', 'micro-frontends', 'shadow-dom', 'css-modules'],
    year: 2025,
  },
  {
    id: 'sd-015',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'mcq',
    question: 'What is the Headless UI pattern and what are its advantages for design system flexibility?',
    options: [
      'Headless UI means components with no visual styling at all — just HTML',
      'Headless UI components provide behavior, accessibility, and state management with no visual styles — consumers bring their own styling via className props or render functions. Examples: Radix UI, Headless UI by Tailwind, React Aria, TanStack Table. Enables one accessible behavior layer with any visual design.',
      'Headless UI is only for server-rendered applications',
      'Headless UI requires Web Components and Shadow DOM',
    ],
    answer: 1,
    explanation: 'Headless pattern separates concerns: behavior/a11y (handled by library) vs visual design (handled by consumer). Benefits: (1) Full visual flexibility — style with Tailwind, CSS Modules, or styled-components; (2) Accessibility handled correctly by experts; (3) Design system can have a distinct look per product while sharing behavior; (4) Easier to upgrade styling without breaking behavior. Radix UI + Tailwind is the most popular 2024-2025 combination.',
    tags: ['headless-ui', 'component-patterns', 'design-system'],
    year: 2025,
  },
  {
    id: 'sd-016',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'system-design',
    question: 'How would you design a frontend authentication architecture for a Next.js app that supports multiple auth providers (Google, GitHub, magic link) with session management and protected routes?',
    options: [
      'Store JWT tokens in localStorage and check them on every page',
      'Use Auth.js (NextAuth v5): provider config (OAuth, magic link), session stored in httpOnly secure cookies (not localStorage — XSS protection), middleware-based route protection, session available in Server Components via auth() helper, JWT vs database session strategies based on scaling needs.',
      'Build custom authentication from scratch for full control',
      'Use a separate authentication service on a subdomain with cross-origin cookies',
    ],
    answer: 1,
    explanation: 'Auth.js (NextAuth v5) architecture: (1) Provider config: OAuth (Google, GitHub) + email magic link. (2) Session: httpOnly cookies with CSRF protection — never localStorage. (3) Middleware: protect /dashboard/* routes by checking session before page render. (4) Server Components: auth() returns session directly. (5) Client: useSession() hook for session-aware client components. (6) Database adapter (Prisma) for persistent sessions if needed.',
    tags: ['authentication', 'nextauth', 'nextjs', 'security'],
    year: 2025,
  },
  {
    id: 'sd-017',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'mcq',
    question: 'What is the key difference between horizontal and vertical code splitting in a large React application?',
    options: [
      'Horizontal splits by file size; vertical splits by component count',
      'Vertical splitting (route-based): split by application routes/features — the /checkout page bundle is separate from /profile. Horizontal splitting (component-based): split by component complexity within a route — lazy load the heavy charting library only when the chart renders. Combine both: route splits for navigation, component splits for heavy within-page features.',
      'Horizontal is for CSS; vertical is for JavaScript',
      'They are the same concept with different naming conventions',
    ],
    answer: 1,
    explanation: 'Route-level (vertical) splitting is the baseline — automatic in Next.js App Router. Within a page, horizontal splitting targets heavy components: const Chart = lazy(() => import("recharts-chart")). This pattern applies to: rich text editors, PDF viewers, 3D renderers, large data visualization libraries. The combined approach gives the minimum initial payload per route while lazy-loading heavy features on demand.',
    tags: ['code-splitting', 'lazy-loading', 'architecture'],
    year: 2025,
  },
  {
    id: 'sd-018',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'true-false',
    question: 'In a design system, semantic tokens (like color-background-primary) should reference primitive tokens (like color-gray-50) rather than raw values, enabling theme changes by updating only the semantic layer.',
    answer: true,
    explanation: 'The token hierarchy is: (1) Primitive/Global tokens hold raw values: --color-gray-50: #f9fafb. (2) Semantic tokens reference primitives: --color-background-primary: var(--color-gray-50). (3) Dark mode: override semantic tokens in [data-theme="dark"] { --color-background-primary: var(--color-gray-950) }. Components use semantic tokens — no component needs to change when you add dark mode.',
    tags: ['design-tokens', 'theming', 'design-system'],
    year: 2025,
  },
  {
    id: 'sd-019',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'mcq',
    question: 'How would you handle feature flags in a large frontend application to enable gradual rollouts and A/B testing?',
    options: [
      'Use environment variables in .env files for all feature flags',
      'Use a feature flag service (LaunchDarkly, GrowthBook, or custom): flags fetched server-side (for RSC/SSR — prevents flash) or client-side (for real-time). User segmentation (userId hash for percentage rollouts). Type-safe flag evaluation with TypeScript. Edge evaluation at CDN for zero-latency flag checks.',
      'Use if (process.env.NODE_ENV === "development") for all feature flags',
      'Hard-code feature flag values and create separate deployments per flag combination',
    ],
    answer: 1,
    explanation: 'Production feature flag architecture: (1) Service (not env vars) for real-time updates without redeploy. (2) Edge evaluation: middleware reads flags from KV store, adds cookie/header, server components read flag value from request — no client round-trip needed. (3) Type safety: generated TypeScript types for all flag names/values. (4) Analytics: track impression + conversion per variant. (5) Cleanup: automated reminder to remove flags after experiment end.',
    tags: ['feature-flags', 'ab-testing', 'architecture', 'rollouts'],
    year: 2025,
  },
  {
    id: 'sd-020',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'mcq',
    question: 'What is event-driven architecture in a frontend context and how does it enable decoupled micro-frontend communication?',
    options: [
      'Event-driven means using addEventListener for all user interactions',
      'Frontend event bus: micro-frontends emit and listen to custom DOM events (or a pub/sub library). MFE-A dispatches CustomEvent("user:logged-in", detail) on window. MFE-B listens for user:logged-in events. Neither MFE has a direct import dependency on the other — coupling is via event schema only.',
      'Event-driven architecture requires WebSockets for all communication',
      'Event sourcing is the same as event-driven frontend architecture',
    ],
    answer: 1,
    explanation: 'Micro-frontend event bus: custom events on window/document work natively without libraries. Define a typed event catalog (TypeScript interface per event). MFEs register listeners on mount, remove on unmount. Benefits: zero coupling (no shared imports), simple debugging (DevTools event listener panel). Considerations: events are fire-and-forget (no return value), avoid for synchronous operations, use for notifications (user logged in, cart updated, navigation requested).',
    tags: ['event-driven', 'micro-frontends', 'communication', 'decoupling'],
    year: 2025,
  },
  {
    id: 'sd-021',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'mcq',
    question: 'What is the BFF (Backend for Frontend) pattern and what problem does it solve for frontend teams?',
    options: [
      'BFF stands for "Best Feature First" — a development prioritization strategy',
      'BFF is a dedicated backend layer owned by the frontend team that aggregates, transforms, and optimizes API responses for specific frontend needs — avoiding over-fetching, adapting mobile vs desktop payloads, and giving frontend teams control over their data contracts without backend changes.',
      'BFF is a security pattern for preventing CORS issues',
      'BFF is a caching layer that sits between the frontend and database',
    ],
    answer: 1,
    explanation: 'BFF pattern: instead of calling 3 microservices and assembling data client-side (slow, complex, exposes internal APIs), a BFF server makes all calls and returns one optimized response for the UI. Frontend team owns the BFF. Benefits: tailored payloads (mobile BFF returns fewer fields), single authentication boundary, hide microservice complexity from frontend, enable server-side composition. Next.js API routes or separate Node.js service serve as BFF.',
    tags: ['bff', 'api-design', 'architecture', 'backend-for-frontend'],
    year: 2025,
  },
  {
    id: 'sd-022',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'system-design',
    question: 'How would you design the observability (monitoring, logging, error tracking) for a production React application?',
    options: [
      'Add console.log statements throughout the codebase',
      'Stack: (1) Error tracking: Sentry with source maps for stack traces to original code; React ErrorBoundary feeds to Sentry. (2) Real User Monitoring (RUM): Core Web Vitals via web-vitals library → analytics. (3) Distributed tracing: OpenTelemetry for frontend → BFF → microservices trace correlation. (4) Session replay: LogRocket/PostHog for debugging complex user issues.',
      'Rely on browser DevTools for production debugging',
      'Use server logs only — frontend errors are not important to track',
    ],
    answer: 1,
    explanation: 'Frontend observability layers: (1) Sentry: automatic JS error capture, performance tracing, source map upload for readable stack traces. (2) web-vitals: report LCP/INP/CLS to analytics/RUM. (3) Custom error boundaries: catch React render errors, add user context before sending to Sentry. (4) Feature flag integration: add active flags to Sentry context for debugging. (5) Alerts: P95 INP regression alerts in CI/CD. Map errors to deployments with release tracking.',
    tags: ['observability', 'sentry', 'monitoring', 'error-tracking'],
    year: 2025,
  },
  {
    id: 'sd-023',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'mcq',
    question: 'What is the "Props Drilling" problem and what are the recommended solutions in modern React?',
    options: [
      'Props drilling is when components receive too many props and become slow',
      'Props drilling is passing data through many intermediate components that don\'t use it. Solutions: (1) React Context for global/cross-cutting data (theme, auth user); (2) Component composition (lift state up and pass JSX as children to avoid intermediate hops); (3) Zustand/Jotai for state accessed by deeply nested components without Provider boilerplate.',
      'Props drilling is when you use both controlled and uncontrolled components',
      'Props drilling can be solved by converting all components to Class Components',
    ],
    answer: 1,
    explanation: 'Solving props drilling — ordered by recommendation: (1) Composition first: instead of passing data down, pass the consuming component as children — no intermediate props needed. (2) Context: for truly cross-cutting concerns (theme, locale, auth). Avoid for high-frequency state changes (performance). (3) State management library (Zustand atom accessed directly) for complex, widely-shared client state. Never default to Context for all shared state.',
    tags: ['props-drilling', 'context', 'composition', 'react'],
    year: 2025,
  },
  {
    id: 'sd-024',
    topic: 'system-design-fe',
    difficulty: 'junior',
    type: 'mcq',
    question: 'What is Server-Side Rendering (SSR) vs Static Site Generation (SSG) and when do you choose each?',
    options: [
      'SSR is for dynamic pages; SSG is for login pages',
      'SSR renders HTML on each request (real-time data, user-specific content — dashboards, feeds). SSG pre-renders HTML at build time (content that rarely changes — blogs, docs, marketing pages). ISR combines both: pre-rendered pages that regenerate in the background.',
      'SSG requires a Node.js server; SSR works without one',
      'SSR is deprecated in favor of RSC; SSG is deprecated in favor of PPR',
    ],
    answer: 1,
    explanation: 'Decision matrix: SSG = same content for all users + changes infrequently → blogs, docs, landing pages (best performance, CDN-cached). SSR = different content per user or request + real-time data → dashboards, feed, authenticated pages. ISR = SSG + periodic refresh → e-commerce product pages, news. PPR (Next.js 15) blurs the line: static shell + streaming dynamic content.',
    tags: ['ssr', 'ssg', 'isr', 'rendering-strategy'],
    year: 2025,
  },
  {
    id: 'sd-025',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'mcq',
    question: 'What considerations go into designing a component API that is both flexible enough for edge cases and opinionated enough for consistent usage across a team?',
    options: [
      'Accept all possible configurations as props and let consumers figure it out',
      'Apply "escape hatches, not configuration explosion": design for the 80% use case with sensible defaults, expose className/style for style overrides, as prop for polymorphic elements, and ref forwarding for imperative access. Avoid boolean flag proliferation — use variant patterns (variant="primary|secondary") instead of separate isPrimary/isSecondary booleans.',
      'Lock down components completely with no customization allowed',
      'Generate all component variants at build time using code generation',
    ],
    answer: 1,
    explanation: 'Component API design principles: (1) Sensible defaults — works well out of the box. (2) Variant patterns (CVA) — type-safe variant combinations instead of boolean props explosion. (3) Composition escape hatches — asChild prop (Radix) lets consumers render as their own element. (4) ref forwarding — required for animation libraries and focus management. (5) TypeScript discriminated union props for mutually exclusive options. (6) No internal state duplication — if something should be controlled/uncontrolled, support both.',
    tags: ['api-design', 'component-design', 'design-system'],
    year: 2025,
  },
  {
    id: 'sd-026',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'mcq',
    question: 'What is edge computing in the frontend context, and how do Cloudflare Workers and Vercel Edge Functions differ from traditional serverless functions?',
    options: [
      'Edge functions are just serverless functions with a different name',
      'Edge functions run in data centers geographically close to the user (not a centralized region), enabling sub-10ms response times. They are limited to the Web APIs subset (no Node.js built-ins), run V8 isolates instead of containers, have cold start near zero, and are ideal for: auth middleware, A/B testing, geo-routing, and personalized HTML responses.',
      'Edge computing means running code inside the browser',
      'Edge functions replace CDNs entirely',
    ],
    answer: 1,
    explanation: 'Edge runtime differences from Node.js serverless: (1) Runtime: V8 isolates (not Node.js) — no fs, no native modules, limited APIs. (2) Geography: 200+ PoPs globally vs single region. (3) Cold start: ~0ms vs 100-500ms. (4) Use cases: request rewriting, auth token validation, feature flag evaluation, localization, bot protection. Vercel Edge Middleware uses Next.js middleware at the edge. Cloudflare Workers have KV, R2, D1 for persistence at the edge. Trade-off: limited compute time and no Node.js APIs.',
    tags: ['edge-computing', 'cloudflare-workers', 'vercel-edge', 'serverless'],
    year: 2025,
  },
  {
    id: 'sd-027',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'mcq',
    question: 'What is the difference between Server-Sent Events (SSE), WebSockets, and WebTransport for real-time frontend data?',
    options: [
      'They are all the same technology with different names',
      'SSE: unidirectional server→client over HTTP, simple EventSource API, auto-reconnect, text-only. WebSocket: full-duplex bidirectional over a persistent TCP connection, binary and text, requires ws:// protocol. WebTransport: modern bidirectional over HTTP/3 QUIC, supports multiple streams, lower latency than WebSocket, still experimental.',
      'WebSockets are always better — use them for everything',
      'SSE requires a special server that supports the MQTT protocol',
    ],
    answer: 1,
    explanation: 'Choosing a streaming protocol: SSE — simplest, works through HTTP/2 multiplexing, ideal for notification feeds, live logs, AI streaming responses. WebSocket — for interactive real-time apps (chat, collaborative editing, live games) where client also sends data frequently. WebTransport — for ultra-low-latency use cases (gaming, live media) over HTTP/3; not yet widely deployed. SSE is underrated: it uses standard HTTP, benefits from CDN, and works with fetch() via ReadableStream as well as EventSource.',
    tags: ['sse', 'websocket', 'webtransport', 'real-time', 'streaming'],
    year: 2025,
  },
  {
    id: 'sd-028',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'system-design',
    question: 'Design the frontend architecture for a multi-tenant SaaS application where each tenant has custom branding, feature flags, and potentially different UI layouts.',
    options: [
      'Create a separate deployment per tenant',
      'Single codebase with tenant context: load tenant config (branding tokens, enabled features, layout variant) from API on app init based on subdomain/domain. CSS custom properties for theming, feature flag service for capabilities, tenant-specific route guards. All tenants share the same bundle; tenant config drives appearance and behavior.',
      'Use iframes to embed different tenant dashboards',
      'Generate a new React app build for each tenant on sign-up',
    ],
    answer: 1,
    explanation: 'Multi-tenant SaaS frontend: (1) Tenant resolution: subdomain (tenant.app.com) or custom domain → lookup tenant config at edge. (2) Branding: CSS custom properties loaded dynamically from tenant config (--color-primary, --logo-url) — no per-tenant CSS files. (3) Features: feature flag service returns enabled capabilities per tenant; components check useFeatureFlag(). (4) Layout: tenant config includes layoutVariant ("sidebar" | "topnav") — component switches rendering. (5) Data isolation: all API requests include tenant ID via header or JWT claim. One codebase, infinite tenants.',
    tags: ['multi-tenant', 'saas', 'theming', 'feature-flags', 'architecture'],
    year: 2025,
  },
  {
    id: 'sd-029',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'mcq',
    question: 'How would you design a versioning and distribution strategy for a design system consumed by 10+ product teams?',
    options: [
      'All teams always use the latest version — no versioning needed',
      'Semantic versioning (semver) with a clear breaking change policy: major = breaking API changes, minor = new components/features, patch = bug fixes. Publish to npm (private or public), maintain a changelog and migration guides, use peer deps for React version, provide codemods for major migrations, and support at least N-1 major version.',
      'Use git submodules instead of npm packages',
      'Each team forks the design system and maintains their own copy',
    ],
    answer: 1,
    explanation: 'Design system versioning strategy: (1) Semver: major breaks force migration — provide automated codemods (jscodeshift). (2) Changelog: every PR requires a changeset (Changesets tool automates release notes). (3) Support policy: support current + previous major; security fixes backported. (4) Distribution: npm package with tree-shakeable ESM builds, CSS exports, TypeScript types. (5) Consumer communication: breaking change RFC process, deprecation warnings in console before removal. (6) Visual regression tests (Chromatic) prevent unintended visual breaking changes even in minor/patch versions.',
    tags: ['design-system', 'versioning', 'semver', 'npm', 'distribution'],
    year: 2025,
  },
  {
    id: 'sd-030',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'mcq',
    question: 'What is an error boundary strategy at scale in a large React application, and how should you layer them?',
    options: [
      'Place one error boundary at the app root to catch everything',
      'Layer boundaries at multiple granularities: (1) App-level: catch crashes, show full-page error; (2) Route-level: per page fallback; (3) Widget-level: isolate independent features so one widget crash does not take down the page; (4) Async boundary: pair with Suspense. Each layer reports to Sentry with context (route, user, component).',
      'Use try/catch inside each component instead of error boundaries',
      'Error boundaries are only needed for third-party component libraries',
    ],
    answer: 1,
    explanation: 'Error boundary hierarchy: App boundary (lowest granularity) catches everything but shows a blank page. Better: route-level boundaries show a per-page error with a "reload" CTA. Widget-level boundaries isolate feature failures — a broken recommendations widget does not crash checkout. Implementation: create a reusable ErrorBoundary class component wrapping react-error-boundary library. Each boundary: logs to Sentry with componentStack, adds breadcrumbs (route, userId, featureFlags). Pair with Suspense for async: <ErrorBoundary><Suspense fallback><AsyncWidget /></Suspense></ErrorBoundary>.',
    tags: ['error-boundary', 'sentry', 'react', 'resilience', 'architecture'],
    year: 2025,
  },
  {
    id: 'sd-031',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'mcq',
    question: 'What is the BroadcastChannel API and how does it enable state synchronization across browser tabs?',
    options: [
      'BroadcastChannel is a TV/radio streaming API for media applications',
      'BroadcastChannel allows same-origin browser contexts (tabs, iframes, workers) to send messages to each other. Use it to sync: logout (close all tabs on sign out), theme changes, cart updates, and real-time notifications across tabs without a server round-trip.',
      'BroadcastChannel replaces WebSockets for server communication',
      'BroadcastChannel only works between iframes on the same page',
    ],
    answer: 1,
    explanation: 'BroadcastChannel usage: const channel = new BroadcastChannel("auth"); channel.postMessage({ type: "logout" }); — all other tabs on the same origin receive this. Implementation patterns: (1) Auth sync: on logout, broadcast → all tabs redirect to /login. (2) Cart sync: item added in one tab → broadcast → other tabs update cart count. (3) Settings sync: theme change propagates instantly. Compare to localStorage events (storage event) which also broadcast changes but BroadcastChannel is more explicit and structured. Works in Web Workers too.',
    tags: ['broadcast-channel', 'tab-sync', 'cross-tab', 'state-sync'],
    year: 2025,
  },
  {
    id: 'sd-032',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'mcq',
    question: 'What is the difference between progressive enhancement and graceful degradation as frontend architecture strategies?',
    options: [
      'They are opposite names for the same strategy',
      'Progressive enhancement: start with a working baseline (HTML, no JS) and layer enhancements (CSS, JS) on top — the core functionality works without JavaScript. Graceful degradation: build the full experience first, then add fallbacks for when features are unavailable. Progressive enhancement is more resilient; graceful degradation is more pragmatic for complex apps.',
      'Progressive enhancement is for mobile; graceful degradation is for desktop',
      'Graceful degradation means disabling features for slow devices only',
    ],
    answer: 1,
    explanation: 'Progressive enhancement philosophy: HTML form works without JS → add JS for client-side validation and async submission. Works when JS fails, CDN is blocked, or users have JS disabled. Graceful degradation: build the interactive SPA, then add <noscript> fallbacks. In practice, 2025 React apps lean toward progressive enhancement via: Next.js Server Actions (forms work without client-side JS), semantic HTML buttons (keyboard accessible even if CSS fails), and feature detection over browser sniffing. The choice affects resilience and accessibility fundamentally.',
    tags: ['progressive-enhancement', 'graceful-degradation', 'resilience', 'architecture'],
    year: 2025,
  },
  {
    id: 'sd-033',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'system-design',
    question: 'How would you design a CDN architecture and cache invalidation strategy for a Next.js application with both static and dynamic content?',
    options: [
      'Set all responses to Cache-Control: no-cache to always get fresh content',
      'Layered caching: (1) Static assets (JS/CSS bundles) — content-hashed filenames, immutable cache (max-age=31536000, immutable); (2) Static pages (SSG) — long TTL with on-demand revalidation (ISR) or deploy-time purge; (3) Dynamic pages — short TTL or stale-while-revalidate; (4) API responses — Vary by auth state, short TTL or cache at edge with purge on mutation.',
      'Use a single CDN rule: cache everything for 1 hour',
      'Skip CDN for Next.js — Vercel handles caching automatically',
    ],
    answer: 1,
    explanation: 'CDN cache strategy per content type: (1) JS/CSS bundles: content hash in filename → max-age=31536000, immutable — safe to cache forever, new deploy = new hash. (2) HTML pages: stale-while-revalidate=60, or ISR revalidate on-demand via revalidatePath(). (3) API responses: Cache-Control: s-maxage=60, stale-while-revalidate=3600 for public data; private for user-specific. (4) Cache invalidation: on data mutation, call CDN purge API (Cloudflare/Vercel purge by tag). Surrogate-Cache-Control (Vercel) or Cache-Tag (Cloudflare) enables granular tag-based purging without full cache bust.',
    tags: ['cdn', 'cache-invalidation', 'isr', 'nextjs', 'cache-control'],
    year: 2025,
  },
  {
    id: 'sd-034',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'true-false',
    question: 'WebTransport over HTTP/3 is currently the recommended replacement for WebSockets in all production real-time web applications.',
    answer: false,
    explanation: 'WebTransport is promising for ultra-low-latency use cases (gaming, live media) due to HTTP/3/QUIC benefits (no head-of-line blocking, 0-RTT connection establishment), but as of 2025 it is still not universally recommended for all production apps. Reasons: (1) Browser support is still maturing. (2) Server infrastructure (HTTP/3 support) is not universal. (3) WebSockets have a massive ecosystem (Socket.io, ws library), battle-tested reliability, and work through most corporate proxies. Use WebTransport for specialized high-performance scenarios; use WebSockets for standard real-time collaboration and chat.',
    tags: ['webtransport', 'websocket', 'http3', 'real-time', 'production'],
    year: 2025,
  },
  {
    id: 'sd-035',
    topic: 'system-design-fe',
    difficulty: 'junior',
    type: 'mcq',
    question: 'What is the purpose of a Service Worker in a Progressive Web App (PWA) architecture?',
    options: [
      'A background thread that handles React component rendering',
      'A scriptable network proxy that runs in the background: intercepts network requests, serves cached responses when offline, enables background sync, push notifications, and pre-caching of assets — making the app installable and functional without network connectivity.',
      'A web worker dedicated to processing service API calls',
      'A Node.js server process bundled into the app',
    ],
    answer: 1,
    explanation: 'Service Worker capabilities: (1) Network proxy: intercept fetch requests → serve from cache or network (cache-first, network-first strategies via Workbox). (2) Pre-caching: install event caches app shell — loads instantly offline. (3) Background Sync: queue failed requests, retry on reconnect. (4) Push notifications: receive push events and show notifications even when app is closed. (5) Periodic background sync: update content in the background. Service Workers have a lifecycle (install → activate → fetch) and scope limited to their origin path. Workbox abstracts the low-level caching strategies.',
    tags: ['service-worker', 'pwa', 'offline', 'caching', 'workbox'],
    year: 2025,
  },
  {
    id: 'sd-036',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'code-output',
    question: 'What does this BroadcastChannel code do when a user logs out in one tab?',
    code: `// auth.ts
const authChannel = new BroadcastChannel('auth-sync')

export function logout() {
  clearTokens()
  authChannel.postMessage({ type: 'LOGOUT' })
  window.location.href = '/login'
}

// In App root component
useEffect(() => {
  const authChannel = new BroadcastChannel('auth-sync')
  authChannel.onmessage = (event) => {
    if (event.data.type === 'LOGOUT') {
      clearTokens()
      window.location.href = '/login'
    }
  }
  return () => authChannel.close()
}, [])`,
    answer: 'When logout() is called in one tab, it clears tokens and broadcasts a LOGOUT message. All other open tabs on the same origin receive the message via the BroadcastChannel listener and are also redirected to /login — achieving synchronized logout across all tabs.',
    explanation: 'This is the canonical cross-tab auth synchronization pattern. The broadcasting tab handles its own redirect after postMessage(). All other tabs receive onmessage, clear their local tokens, and redirect. The useEffect cleanup closes the channel to prevent memory leaks. Note: BroadcastChannel only communicates between same-origin contexts (same protocol + domain + port). The channel name ("auth-sync") must match on both sides. This pattern also applies to token refresh — broadcast the new token to all tabs so they do not each trigger a separate refresh.',
    tags: ['broadcast-channel', 'auth', 'cross-tab', 'logout', 'tab-sync'],
    year: 2025,
  },
  {
    id: 'sd-037',
    topic: 'system-design-fe',
    difficulty: 'mid',
    type: 'true-false',
    question: 'In a micro-frontend architecture using Module Federation, all micro-frontends must use the same version of React to avoid runtime errors.',
    answer: false,
    explanation: 'Module Federation supports sharing dependencies as singletons with version negotiation. By configuring shared: { react: { singleton: true, requiredVersion: "^18.0.0" } }, Module Federation ensures only one React instance runs, even if different MFEs declare slightly different compatible versions. If versions are incompatible, it falls back to loading separate instances (which can cause issues with hooks and context). Best practice: use singleton: true for peer dependencies (React, ReactDOM, React Router) and specify requiredVersion. This enables gradual React version upgrades across MFEs without a coordinated big-bang update.',
    tags: ['module-federation', 'micro-frontends', 'react', 'shared-dependencies'],
    year: 2025,
  },
  {
    id: 'sd-038',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'mcq',
    question: 'How would you implement a real-time collaborative presence system (showing who is viewing/editing) in a frontend application?',
    options: [
      'Poll the server every second to check active users',
      'Use a dedicated presence channel (WebSocket or Yjs Awareness): clients send heartbeats with their cursor position and user info; server broadcasts presence updates to all connected clients. Client maintains a presence map, removes users after heartbeat timeout. Render each user\'s cursor/avatar in the UI with smooth interpolation.',
      'Use localStorage to store active users and check it periodically',
      'Show presence only when users explicitly click an "announce presence" button',
    ],
    answer: 1,
    explanation: 'Presence system architecture: (1) Connection: on page load, client joins a presence channel (room ID). (2) Heartbeat: client sends presence updates (userId, name, avatar, cursor position) every 2-5 seconds. (3) Server: broadcasts presence state to all room members. (4) Timeout: remove user from presence map if no heartbeat for 10-15 seconds. (5) UI: render avatars in toolbar, cursors on canvas with smooth CSS transitions. Yjs Awareness API handles all this: awareness.setLocalState({ user, cursor }) — automatically synced to all peers. Liveblocks and PartyKit offer hosted presence infrastructure.',
    tags: ['presence', 'real-time', 'collaboration', 'websocket', 'yjs'],
    year: 2025,
  },
  {
    id: 'sd-039',
    topic: 'system-design-fe',
    difficulty: 'senior',
    type: 'mcq',
    question: 'What is Partial Prerendering (PPR) in Next.js 15 and how does it change the SSR vs SSG trade-off?',
    options: [
      'PPR is a way to prerender only half the components on a page',
      'PPR renders a static shell at build time (instant from CDN) while streaming dynamic content into Suspense boundaries at request time — combining the performance of SSG with the data freshness of SSR in a single page, without choosing one or the other.',
      'PPR is the same as ISR with a shorter revalidation period',
      'PPR only works with Vercel\'s infrastructure, not self-hosted Next.js',
    ],
    answer: 1,
    explanation: 'PPR architecture: the static shell (header, layout, navigation) is HTML prerendered at build time and served from CDN edge with zero server latency. Dynamic sections (user data, personalized content, live data) are wrapped in <Suspense> boundaries and streamed from the server as they resolve. The browser receives the static shell immediately, shows skeleton/loading UI for dynamic parts, then streams in the data. This eliminates the choice between "fast but stale" (SSG) and "fresh but slow" (SSR). Enabled per-route: export const experimental_ppr = true in Next.js 15.',
    tags: ['ppr', 'nextjs', 'partial-prerendering', 'streaming', 'ssr'],
    year: 2025,
  },
  {
    id: 'sd-040',
    topic: 'system-design-fe',
    difficulty: 'lead',
    type: 'system-design',
    question: 'How would you design the frontend architecture for a high-traffic e-commerce product listing page that needs to balance SEO, performance, and real-time inventory/pricing?',
    options: [
      'Full client-side rendering with useEffect data fetching',
      'PPR or ISR hybrid: prerender product grid and static content (SEO-critical) at build time / on a short revalidation cycle. Stream or client-fetch real-time inventory/price badges separately. Edge caching for the static shell, CDN cache tags per product for targeted invalidation on stock changes.',
      'Server-side render every request to always show fresh data',
      'Use a separate static site for SEO and a SPA for logged-in users',
    ],
    answer: 1,
    explanation: 'E-commerce listing page architecture: (1) Product grid: ISR (revalidate: 60) — pre-rendered with product names/images served from CDN, great for SEO crawlers. (2) Pricing: client-fetched or streamed separately — prices change frequently, can vary by user segment, should not block page render. (3) Inventory badges ("Only 3 left"): streamed RSC or client SWR with short TTL. (4) Cache invalidation: tag product pages by category/productId → purge on inventory update via webhook. (5) A/B testing: edge middleware splits traffic before HTML is rendered, no client-side flicker.',
    tags: ['e-commerce', 'isr', 'ppr', 'cdn', 'architecture', 'performance'],
    year: 2025,
  },
]
